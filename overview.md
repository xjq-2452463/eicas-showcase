# 项目概述

<div class="hero-badge">
  <span class="badge badge-blue">C++</span>
  <span class="badge badge-green">EasyX</span>
  <span class="badge badge-amber">实时仿真</span>
  <span class="badge badge-red">航电系统</span>
</div>

<style>
.hero-badge {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  margin: 1rem 0 2rem;
}
.badge {
  display: inline-block;
  padding: 4px 12px;
  border-radius: 20px;
  font-size: 0.85rem;
  font-weight: 600;
}
.badge-blue { background: linear-gradient(135deg, #0066cc, #0088ff); color: white; }
.badge-green { background: linear-gradient(135deg, #10b981, #34d399); color: white; }
.badge-amber { background: linear-gradient(135deg, #f59e0b, #fbbf24); color: white; }
.badge-red { background: linear-gradient(135deg, #ef4444, #f87171); color: white; }

.video-wrapper {
  margin: 2rem 0;
  border-radius: 16px;
  overflow: hidden;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
  background: #000;
}

.feature-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 20px;
  margin: 2rem 0;
}

.feature-card {
  padding: 24px;
  background: var(--vp-c-bg-soft);
  border-radius: 12px;
  border: 1px solid var(--vp-c-divider);
  transition: all 0.3s ease;
}

.feature-card:hover {
  border-color: var(--vp-c-brand-1);
  transform: translateY(-4px);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
}

.feature-icon {
  font-size: 2rem;
  margin-bottom: 12px;
}

.feature-title {
  font-size: 1.1rem;
  font-weight: 700;
  color: var(--vp-c-text-1);
  margin-bottom: 8px;
}

.feature-desc {
  color: var(--vp-c-text-2);
  font-size: 0.95rem;
  line-height: 1.7;
}

.highlight-box {
  background: linear-gradient(135deg, rgba(0, 102, 204, 0.1), rgba(0, 212, 255, 0.05));
  border-left: 4px solid var(--vp-c-brand-1);
  padding: 20px 24px;
  border-radius: 0 12px 12px 0;
  margin: 1.5rem 0;
}

.highlight-box p {
  margin: 0;
  color: var(--vp-c-text-1);
}
</style>

本项目是一个基于 **C++** 和 **EasyX 图形库** 开发的**航空发动机指示与机组警告系统 (EICAS)** 仿真器。

<div class="highlight-box">
  <p>💡 <strong>设计理念</strong>：不同于普通的游戏界面，本项目旨在<strong>从底层逻辑上</strong>复刻现代客机航电系统的运行机制，包含完整的物理建模、传感器冗余设计以及黑匣子数据记录功能。</p>
</div>

---

## 🎬 功能演示 (Demo)

> 点击下方播放按钮查看双发启动、推力控制及故障告警演示。

<div class="video-wrapper">
  <video controls width="100%" height="auto">
    <source src="./video.mp4" type="video/mp4">
    您的浏览器不支持 Video 标签。
  </video>
</div>

---

## ✨ 核心系统特性

<div class="feature-grid">
  <div class="feature-card">
    <div class="feature-icon">📈</div>
    <div class="feature-title">高保真物理仿真</div>
    <div class="feature-desc">基于航空热力学的非线性数学模型，使用对数函数拟合发动机启动曲线，实现真实的惯性响应与噪声模拟。</div>
  </div>
  <div class="feature-card">
    <div class="feature-icon">⚠️</div>
    <div class="feature-title">故障注入系统</div>
    <div class="feature-desc">支持 14+ 种异常工况的实时注入，包括传感器失效、热启动、超速、燃油泄漏等专业故障模式。</div>
  </div>
  <div class="feature-card">
    <div class="feature-icon">🛡️</div>
    <div class="feature-title">航电冗余逻辑</div>
    <div class="feature-desc">完整复刻 A/B 双通道信号采集、交叉比对与多数表决机制，实现单点故障容错能力。</div>
  </div>
  <div class="feature-card">
    <div class="feature-icon">💾</div>
    <div class="feature-title">数据黑匣子</div>
    <div class="feature-desc">以 200Hz 频率记录全飞行周期参数至 CSV 文件，支持导入专业软件进行飞行包线分析。</div>
  </div>
</div>

---

### 1. 高保真物理仿真 (Physics Engine)

系统通过数学公式**实时计算**发动机状态，而非简单的动画帧播放：

| 仿真特性 | 技术实现 | 效果描述 |
|:---|:---|:---|
| **非线性启动曲线** | 对数函数拟合 | 模拟从 0% 到 58% N1 的慢速爬升，还原涡扇发动机启动惯性 |
| **停车衰减模型** | 指数衰减算法 | 模拟切断燃油后约 10 秒的转子自由旋转惯性 |
| **随机噪声模拟** | ±3% 高斯白噪声 | 模拟真实传感器在稳态运行时的读数波动 |

::: info 为什么选择对数函数？
对数函数能够很好地模拟发动机启动时"快速响应后逐渐趋于稳定"的物理特性——初期转速增长迅速，后期增速放缓并平滑过渡到稳态。
:::

---

### 2. 故障注入系统 (Fault Injection)

集成了专业的故障测试面板，支持 **14+ 种**异常工况的实时注入：

**传感器级故障**
- 左/右发 N1、EGT 传感器的 A/B 通道独立失效
- 支持断路、短路、信号丢失等多种失效模式

**严重机械故障**
- 🔥 **热启动 (Hot Start)** — 启动期间 EGT 飙升至 1000°C 以上
- ⚡ **超速 (Overspeed)** — 转子转速突破 105% 红线
- ⛽ **燃油泄漏 (High Flow)** — 燃油流速异常增高但推力不增加

::: warning 安全机制
当检测到严重故障（如双发全失效、N1 > 120%）时，系统将触发**自动停车保护**，模拟真实飞机的安全联锁逻辑。
:::

---

### 3. 航电冗余逻辑 (Avionics Redundancy)

完整复刻了真实飞机的信号处理链条：

**双通道采集**：每个关键参数（N1, EGT）均由 Sensor A 和 Sensor B 同时采集，并添加随机噪声模拟真实传感器。

**数据融合**：系统计算有效通道的平均值作为显示输出，单路失效时自动切换至备用通道，并触发白色 `SENS FAULT` 告警。

**告警分级**：采用三色告警系统（白色 STATUS / 琥珀色 CAUTION / 红色 WARNING），严重故障触发自动停车保护。

| 信号状态 | 系统响应 | 告警等级 |
|:---|:---|:---|
| A、B 均正常 | 输出 (A+B)/2 | 正常显示 |
| A 失效，B 正常 | 输出 B | ⬜ 白色 STATUS |
| A、B 均失效 | valid = false | 🟨 琥珀色 (单发) / 🟥 红色 (双发) |

---

### 4. 数据黑匣子 (Data Logger)

内置飞行数据记录器（FDR），实现专业级数据存储：

| 记录参数 | 说明 |
|:---|:---|
| **采样频率** | 200Hz (5ms 间隔) |
| **时间戳** | 精确到毫秒级 |
| **传感器原始值** | N1(A/B)、EGT(A/B) 双通道数据 |
| **显示处理值** | 融合后的仪表显示数据 |
| **系统状态** | 燃油流速、燃油总量、告警状态 |

数据文件可直接导入 **Excel** 或 **MATLAB** 进行后处理分析。

---

## 🛠️ 技术栈与环境

| 维度 | 详细说明 |
|:---|:---|
| **开发语言** | C++ (Visual Studio 2022) |
| **图形引擎** | EasyX Graphics Library (基于 Windows GDI) |
| **数学库** | `<cmath>` (log10, random, clamp) |
| **界面交互** | 鼠标消息事件循环 / 矩形碰撞检测 |
| **数据存储** | `std::ofstream` 文件流操作 |

---

## 📁 项目文件结构

| 文件名 | 模块定位 | 核心职责 |
|:---|:---|:---|
| `main.cpp` | **主程序** | 程序入口、主循环、时间步进、输入事件处理、绘图调度 |
| `Logic.cpp` | **逻辑层** | Engine 物理模型、Aircraft 状态机、传感器噪声、告警判定 |
| `UI.cpp` | **视图层** | 仪表盘绘制、按钮渲染、日志面板、告警颜色管理 |
| `Common.h` | **公共定义** | 全局常量、数据结构 (struct/enum)、颜色宏、函数声明 |

::: tip 架构设计
项目采用 **MVC 分离架构**，逻辑层与视图层完全解耦，便于维护和扩展。
:::
